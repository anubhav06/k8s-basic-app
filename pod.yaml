apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-application-2
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
    #securityContext:
    #  allowPrivilegeEscalation: false
    #  readOnlyRootFilesystem: true
    #  runAsGroup: 1000
    #  runAsNonRoot: true
